# vista-frontend

The `vista-frontend` crate is a placeholder frontend, to be replaced by a proper web interface. 
It uses the [`egui`](https://github.com/emilk/egui) framework to provide simple feedback for the
vSLAM pipeline.
